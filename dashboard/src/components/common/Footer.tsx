import { Box, Divider, Grid } from '@mui/material';

export const Footer = () => {
  return (
    <Grid size={{ xs: 12 }}>
      <Box sx={{ mb: 5 }}>
        <Divider />
      </Box>
    </Grid>
  );
};
